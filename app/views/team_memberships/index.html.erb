<h1>Listing team memberships</h1>

<table>
  <tr>
    <th>id</th>
    <th>Team</th>
    <th>Member</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @team_memberships.each_with_index do |team_membership, idx| %>
  <tr>
    <td><%= idx %></td>
    <td><%= team_membership.team.name %></td>
    <td><%= team_membership.member.fullname %></td>
    <td><%= link_to 'Show', team_membership %></td>
    <td><%= link_to 'Edit', edit_team_membership_path(team_membership) %></td>
    <td><%= link_to 'Destroy', team_membership, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team membership', new_team_membership_path %>
